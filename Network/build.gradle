apply plugin: 'java'
apply plugin: 'maven'


dependencies{
    compile 'redis.clients:jedis:2.8.0'
}
publishing {
    publications {
        jar(MavenPublication) {
            from components.java
        }
    }
}